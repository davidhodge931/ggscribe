% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate-axis.R
\name{annotate_axis_bracket}
\alias{annotate_axis_bracket}
\title{Annotate an axis bracket}
\usage{
annotate_axis_bracket(
  ...,
  position = NULL,
  xintercept = NULL,
  yintercept = NULL,
  breaks,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  ticks_length = ggplot2::rel(1)
)
}
\arguments{
\item{...}{Not used. Forces named arguments.}

\item{position}{One of \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"right"}. Inferred
from \code{xintercept} or \code{yintercept} if not provided.}

\item{xintercept}{For \code{"left"}/\code{"right"} axes: float the bracket to this x
position in data coordinates instead of the panel edge.}

\item{yintercept}{For \code{"top"}/\code{"bottom"} axes: float the bracket to this y
position in data coordinates instead of the panel edge.}

\item{breaks}{A numeric vector of length >= 2. The bar spans \code{min(breaks)}
to \code{max(breaks)}; caps are drawn at every break value.}

\item{colour}{Inherits from \code{axis.ticks} in the set theme (falling back
through \code{axis.line} and \code{line}).}

\item{linewidth}{Inherits from \code{axis.ticks} in the set theme. Supports \code{rel()}.}

\item{linetype}{Inherits from \code{axis.ticks} in the set theme.}

\item{ticks_length}{Length of the bracket caps as a grid unit. Supports
\code{rel()}. Negative values flip the cap direction. Defaults to \code{rel(1)}
(outward at theme tick length).}
}
\value{
A list of ggplot2 annotation layers.
}
\description{
Draws a bracket spanning \code{min(breaks)} to \code{max(breaks)} along an axis edge
or at a floating data position. The bar uses the same rendering path as
\code{\link[=annotate_axis_line]{annotate_axis_line()}}; the caps use the same path as \code{\link[=annotate_axis_ticks]{annotate_axis_ticks()}}.
Requires \code{coord_cartesian(clip = "off")}.
}
\seealso{
\code{\link[=annotate_axis_line]{annotate_axis_line()}}, \code{\link[=annotate_axis_ticks]{annotate_axis_ticks()}},
\code{\link[=annotate_axis_text]{annotate_axis_text()}}, \code{\link[=annotate_reference_line]{annotate_reference_line()}},
\code{\link[=annotate_panel_shade]{annotate_panel_shade()}}, \code{\link[=sec_axis_annotate]{sec_axis_annotate()}}
}
