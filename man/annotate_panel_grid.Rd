% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{annotate_panel_grid}
\alias{annotate_panel_grid}
\title{Annotate panel grid lines}
\usage{
annotate_panel_grid(
  ...,
  x = NULL,
  y = NULL,
  xmin = NULL,
  xmax = NULL,
  ymin = NULL,
  ymax = NULL,
  minor = FALSE,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  element_to = "keep"
)
}
\arguments{
\item{...}{Named arguments passed to \code{ggplot2::annotate()}. Support trailing
commas.}

\item{x}{A vector of x-axis breaks for vertical grid lines. Cannot be used
together with \code{y}. Use \code{I()} for normalized coordinates (0-1).}

\item{y}{A vector of y-axis breaks for horizontal grid lines. Cannot be used
together with \code{x}. Use \code{I()} for normalized coordinates (0-1).}

\item{xmin, xmax}{Start and end x positions for horizontal grid lines. Use
\code{I()} for normalized coordinates (0-1). Defaults to \code{-Inf} and \code{Inf}.}

\item{ymin, ymax}{Start and end y positions for vertical grid lines. Use
\code{I()} for normalized coordinates (0-1). Defaults to \code{-Inf} and \code{Inf}.}

\item{minor}{Logical. If \code{TRUE}, uses minor grid theme defaults. Defaults to
\code{FALSE}.}

\item{colour}{Inherits from \code{panel.grid.major} or \code{panel.grid.minor} in the
set theme.}

\item{linewidth}{Inherits from \code{panel.grid.major} or \code{panel.grid.minor} in
the set theme. Supports \code{rel()}.}

\item{linetype}{Inherits from \code{panel.grid.major} or \code{panel.grid.minor} in
the set theme.}

\item{element_to}{One of \code{"keep"}, \code{"transparent"}, or \code{"blank"}. Controls
whether native theme grid lines are suppressed. Defaults to \code{"keep"}.}
}
\value{
A list of ggplot2 annotation layers and theme elements.
}
\description{
Draws grid lines at specified break positions with style defaults taken from
the \code{panel.grid} element of the set theme. Specify \code{x} for vertical lines
or \code{y} for horizontal lines.
}
\examples{
library(ggplot2)

set_theme(theme_minimal())

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point()

# Vertical grid lines at specific x breaks
p + annotate_panel_grid(x = c(2, 3, 4, 5))

# Horizontal grid lines at specific y breaks, native lines suppressed
p + annotate_panel_grid(y = c(10, 20, 30), element_to = "blank")

# Minor vertical grid lines
p + annotate_panel_grid(x = seq(2, 5, by = 0.5), minor = TRUE)

# Partial horizontal lines that don't span the full panel width
p + annotate_panel_grid(y = c(15, 25), xmax = I(0.5), element_to = "blank")
}
