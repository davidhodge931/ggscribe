% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-axis.R
\name{guide_axis}
\alias{guide_axis}
\title{Axis guide with annotation-friendly defaults}
\usage{
guide_axis(..., theme = theme_axis())
}
\arguments{
\item{...}{Additional arguments passed to \code{\link[ggplot2:guide_axis]{ggplot2::guide_axis()}}, such as
\code{title}, \code{check.overlap}, or \code{angle}.}

\item{theme}{A \code{theme} object to style the guide. Defaults to
\code{\link[=theme_axis]{theme_axis()}}, which suppresses ticks and lines.}
}
\value{
A \code{guide} object to be used in a scale's \code{guide} argument or within
\code{\link[=sec_axis]{sec_axis()}}.
}
\description{
A wrapper around \code{\link[ggplot2:guide_axis]{ggplot2::guide_axis()}} that defaults to using
\code{\link[=theme_axis]{theme_axis()}}. This guide is designed to strip away standard axis
furniture (like lines and ticks) while preserving text, making it ideal for
secondary axes used as margin labels.
}
\examples{
library(ggplot2)

# Using the guide directly in a scale
ggplot(mpg, aes(displ, hwy)) +
  geom_point() +
  scale_x_continuous(
    guide = ggscribe::guide_axis(title = "Displacement Label Only")
  )

# The guide is also used internally by sec_axis()
ggplot(mpg, aes(displ, hwy)) +
  geom_point() +
  scale_y_continuous(
    sec.axis = ggscribe::sec_axis(
      breaks = 20,
      labels = "Reference point",
      guide = ggscribe::guide_axis(angle = 90)
    )
  )
}
\seealso{
\code{\link[=sec_axis]{sec_axis()}}, \code{\link[=theme_axis]{theme_axis()}}
}
