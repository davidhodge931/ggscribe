% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dup-axis-text.R
\name{dup_axis_text}
\alias{dup_axis_text}
\title{Duplicate axis with axis text only}
\usage{
dup_axis_text(
  breaks = ggplot2::waiver(),
  labels = ggplot2::derive(),
  elements_to = "transparent",
  ...
)
}
\arguments{
\item{breaks}{One of:
\itemize{
\item \code{NULL} for no breaks
\item \code{\link[ggplot2:waiver]{ggplot2::waiver()}} (default) to inherit breaks from the primary axis
\item A numeric vector of break positions
\item A function that takes the scale limits as input and returns break
positions (e.g. \verb{\\(x) mean(c(x[2], 32))})
}}

\item{labels}{One of:
\itemize{
\item \code{\link[ggplot2:sec_axis]{ggplot2::derive()}} (default) to derive labels from \code{breaks}
\item A character vector of labels, the same length as \code{breaks}
\item A function that takes break positions as input and returns labels
}}

\item{elements_to}{One of \code{"keep"}, \code{"transparent"}, or \code{"blank"}. Controls
whether native theme ticks are suppressed. Defaults to \code{"keep"}.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:sec_axis]{ggplot2::dup_axis()}}.}
}
\value{
A \code{AxisSecondary} object for use in the \code{sec.axis} argument of
\code{scale_x_continuous()} or \code{scale_y_continuous()}.
}
\description{
A wrapper around \code{\link[ggplot2:sec_axis]{ggplot2::dup_axis()}} that creates a secondary axis
displaying only axis text — axis lines and ticks are hidden, making it useful
for placing annotation labels (e.g. region labels alongside a shaded panel).
}
\examples{
library(ggplot2)

ggplot(mpg, aes(x = displ, y = hwy)) +
   geom_point() +
   annotate_panel_shade(ymin = 32) +
   scale_y_continuous(
     sec.axis = dup_axis_text(
       breaks = \(x) mean(c(x[2], 32)),
       labels = "Inefficient",
     )
   )
}
\seealso{
\code{\link[ggplot2:sec_axis]{ggplot2::dup_axis()}}, \code{\link[=annotate_panel_shade]{annotate_panel_shade()}}
}
