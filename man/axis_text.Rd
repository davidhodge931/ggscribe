% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-text.R
\name{axis_text}
\alias{axis_text}
\title{Annotate axis text}
\usage{
axis_text(
  ...,
  position = NULL,
  xintercept = NULL,
  yintercept = NULL,
  breaks,
  labels = NULL,
  colour = NULL,
  size = NULL,
  family = NULL,
  hjust = NULL,
  vjust = NULL,
  angle = 0,
  ticks_length = ggplot2::rel(1)
)
}
\arguments{
\item{...}{Not used. Forces named arguments.}

\item{position}{One of \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"right"}. Inferred
from \code{xintercept} or \code{yintercept} if not provided.}

\item{xintercept}{For \code{"left"}/\code{"right"} axes: float the axis to this x
position in data coordinates instead of the panel edge.}

\item{yintercept}{For \code{"top"}/\code{"bottom"} axes: float the axis to this y
position in data coordinates instead of the panel edge.}

\item{breaks}{A numeric vector of break positions.}

\item{labels}{One of:
\itemize{
\item \code{NULL} (default) to use break values as labels
\item A character vector the same length as \code{breaks}
\item A function taking break values and returning labels
}}

\item{colour}{Inherits from \code{axis.text} in the set theme.}

\item{size}{Inherits from \code{axis.text} in the set theme.}

\item{family}{Inherits from \code{axis.text} in the set theme.}

\item{hjust, vjust}{Justification. Auto-calculated from \code{position} if \code{NULL}.}

\item{angle}{Text rotation angle. Defaults to \code{0}.}

\item{ticks_length}{Offset from the axis edge including tick length and margin.
Supports \code{rel()}. Negative values place labels inside the panel. Defaults
to \code{rel(1)} (theme tick length + text margin).}
}
\value{
A list of ggplot2 annotation layers.
}
\description{
Draws text labels at specified break positions along an axis, with style
defaults taken from the \code{axis.text} element of the set theme. Requires
\code{coord_cartesian(clip = "off")}.
}
\seealso{
\code{\link[=axis_line]{axis_line()}}, \code{\link[=axis_ticks]{axis_ticks()}},
\code{\link[=axis_bracket]{axis_bracket()}}, \code{\link[=reference_line]{reference_line()}},
\code{\link[=panel_shade]{panel_shade()}}, \code{\link[=sec_axis]{sec_axis()}}
}
