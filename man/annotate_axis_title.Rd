% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-title-DEV.R
\name{annotate_axis_title}
\alias{annotate_axis_title}
\title{Annotate an axis title}
\usage{
annotate_axis_title(
  ...,
  position,
  label,
  colour = NULL,
  size = NULL,
  family = NULL,
  angle = NULL,
  hjust = NULL,
  vjust = NULL,
  offset = NULL,
  elements_to = "keep"
)
}
\arguments{
\item{...}{Not used. Allows trailing commas and named-argument style calls.}

\item{position}{One of \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"right"}.}

\item{label}{A single string for the title.}

\item{colour}{Inherits from \code{axis.title} in the set theme.}

\item{size}{Inherits from \code{axis.title} in the set theme.}

\item{family}{Inherits from \code{axis.title} in the set theme.}

\item{angle}{Text rotation. Auto-inferred from \code{position} if \code{NULL}
(0 for top/bottom, 90 for left, -90 for right).}

\item{hjust, vjust}{Justification. Auto-calculated from \code{position} if \code{NULL}.}

\item{offset}{Distance from the panel edge as a \code{\link[grid:unit]{grid::unit()}}. Defaults to
\code{unit(30, "pt")}. Increase if the title overlaps tick labels.}

\item{elements_to}{One of \code{"keep"}, \code{"transparent"}, or \code{"blank"}. Controls
whether the native theme axis title is suppressed. Defaults to \code{"keep"}.}
}
\value{
A list of ggplot2 annotation layers and theme elements.
}
\description{
Draws a title label along a panel edge, with style defaults taken from the
\code{axis.title} element of the set theme. Requires \code{coord_cartesian(clip = "off")}
to render outside the panel boundary.
}
\examples{
library(ggplot2)

set_theme(
  ggrefine::theme_grey(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths  = rep(unit(75, "mm"), 100),
  )
)

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  coord_cartesian(clip = "off")

# Replace the bottom axis title
p + labs(x = NULL) +
  annotate_axis_title(position = "bottom", label = "Weight (1000 lbs)")

# Suppress the native title and annotate instead
p + annotate_axis_title(
  position = "left",
  label    = "Miles per gallon",
  elements_to = "blank"
)

# Custom offset when tick labels are large
p + annotate_axis_title(
  position = "bottom",
  label    = "Weight (1000 lbs)",
  offset   = grid::unit(45, "pt")
)
}
\seealso{
\code{\link[=annotate_axis_text]{annotate_axis_text()}}, \code{\link[=annotate_axis_ticks]{annotate_axis_ticks()}}, \code{\link[=annotate_axis_line]{annotate_axis_line()}}
}
