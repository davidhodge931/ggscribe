% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shade.R
\name{scribe_panel_shade}
\alias{scribe_panel_shade}
\title{Annotate the panel background}
\usage{
scribe_panel_shade(
  ...,
  xmin = -Inf,
  xmax = Inf,
  ymin = -Inf,
  ymax = Inf,
  fill = NULL,
  alpha = 0.25,
  colour = "transparent",
  linewidth = NULL,
  linetype = NULL
)
}
\arguments{
\item{...}{Arguments passed to \code{ggplot2::annotate("rect", ....)} (if normalised coordinates not used). Require named arguments (and support trailing commas).}

\item{xmin}{A value of length 1. Defaults to \code{-Inf}. Use \code{I()} to specify normalized coordinates (0-1).}

\item{xmax}{A value of length 1. Defaults to \code{Inf}. Use \code{I()} to specify normalized coordinates (0-1).}

\item{ymin}{A value of length 1. Defaults to \code{-Inf}. Use \code{I()} to specify normalized coordinates (0-1).}

\item{ymax}{A value of length 1. Defaults to \code{Inf}. Use \code{I()} to specify normalized coordinates (0-1).}

\item{fill}{The fill color to blend with the panel background. Defaults to \code{"#8991A1FF"}. The final rectangle color is created by blending this fill with the current panel background: screen blend for dark backgrounds, multiply blend for light backgrounds.}

\item{alpha}{The transparency of the rectangle. Defaults to \code{0.2} (subtle overlay).}

\item{colour}{The border colour of the rectangle. Defaults to \code{"transparent"}.}

\item{linewidth}{A number. Inherits from the current theme \code{panel.border} linewidth. Supports \code{rel()} for relative sizing.}

\item{linetype}{An integer. Defaults to \code{1}.}
}
\value{
A list containing an annotation annotate.
}
\description{
Annotate a filled rectangle on the panel background.
It is designed to work with a theme that is globally set.
}
