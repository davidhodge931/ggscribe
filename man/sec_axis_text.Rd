% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sec-axis-text.R
\name{sec_axis_text}
\alias{sec_axis_text}
\title{Secondary axis for text annotations}
\usage{
sec_axis_text(
  breaks = function(x) mean(x),
  labels = ggplot2::waiver(),
  name = NULL,
  guide = guide_sec_axis_text(),
  ...
)
}
\arguments{
\item{breaks}{A function or numeric vector giving the break position(s) used
to anchor the text. Defaults to \code{NULL}, which places a single label at the
midpoint of the scale — the mean of the limits for continuous scales.}

\item{labels}{One of:
\itemize{
\item A character vector of labels, the same length as \code{breaks}
\item A function that takes break positions as input and returns labels
}}

\item{name}{The name of the secondary axis. Use \code{\link[ggplot2:waiver]{ggplot2::waiver()}} to
derive the name from the primary axis, or \code{NULL} (default) for no name.}

\item{guide}{A guide object used to render the axis. Defaults to
\code{\link[=guide_sec_axis_text]{guide_sec_axis_text()}}, which makes transparent ticks and lines.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:dup_axis]{ggplot2::dup_axis()}}.}
}
\value{
A \code{AxisSecondary} object for use in the \code{sec.axis} argument of
\code{scale_x_continuous()} or \code{scale_y_continuous()}.
}
\description{
Secondary axis for text annotations
}
\seealso{
\code{\link[=guide_sec_axis_text]{guide_sec_axis_text()}}, \code{\link[=axis_text]{axis_text()}}, \code{\link[=axis_ticks]{axis_ticks()}}, \code{\link[=axis_bracket]{axis_bracket()}}
}
