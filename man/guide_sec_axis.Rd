% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-sec-axis.R
\name{guide_sec_axis}
\alias{guide_sec_axis}
\title{Guide for secondary axis annotation}
\usage{
guide_sec_axis(..., theme = theme_sec_axis())
}
\arguments{
\item{...}{Additional arguments passed to \code{\link[ggplot2:guide_axis]{ggplot2::guide_axis()}}, such as
\code{title}, \code{check.overlap}, or \code{angle}.}

\item{theme}{A \code{theme} object to style the guide. Defaults to
\code{\link[=theme_sec_axis]{theme_sec_axis()}}, which suppresses ticks and lines.}
}
\value{
A \code{guide} object to be used in a scale's \code{guide} argument or within
\code{\link[=sec_axis]{sec_axis()}}.
}
\description{
A wrapper around \code{\link[ggplot2:guide_axis]{ggplot2::guide_axis()}} that defaults to using
\code{\link[=theme_sec_axis]{theme_sec_axis()}}. This guide is designed to strip away standard axis
furniture (like lines and ticks) while preserving text, making it ideal for
secondary axes used as margin labels.
}
\examples{
library(ggplot2)

ggplot(mpg, aes(displ, hwy)) +
  ggscribe::reference_line(yintercept = 20) +
  geom_point() +
  scale_y_continuous(
    sec.axis = ggscribe::sec_axis(
      breaks = 20,
      labels = "Reference",
      guide = ggscribe::guide_sec_axis(angle = 270)
    )
  )

}
\seealso{
\code{\link[=sec_axis]{sec_axis()}}, \code{\link[=theme_sec_axis]{theme_sec_axis()}}
}
