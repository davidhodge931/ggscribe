% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-ticks.R
\name{axis_ticks}
\alias{axis_ticks}
\title{Annotate axis ticks}
\usage{
axis_ticks(
  ...,
  position = NULL,
  xintercept = NULL,
  yintercept = NULL,
  breaks,
  minor = FALSE,
  colour = NULL,
  linewidth = NULL,
  length = ggplot2::rel(1)
)
}
\arguments{
\item{...}{Not used. Forces named arguments.}

\item{position}{One of \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"right"}. Inferred
from \code{xintercept} or \code{yintercept} if not provided.}

\item{xintercept}{For \code{"left"}/\code{"right"} axes: float the axis to this x
position in data coordinates instead of the panel edge.}

\item{yintercept}{For \code{"top"}/\code{"bottom"} axes: float the axis to this y
position in data coordinates instead of the panel edge.}

\item{breaks}{A numeric vector of break positions.}

\item{minor}{Logical. If \code{TRUE}, uses minor tick theme defaults. Defaults to
\code{FALSE}.}

\item{colour}{Inherits from \code{axis.ticks} in the set theme.}

\item{linewidth}{Inherits from \code{axis.ticks} in the set theme. Supports \code{rel()}.}

\item{length}{Total tick length as a grid unit. Supports \code{rel()}.
Negative values flip the tick direction (inward). Defaults to \code{rel(1)}
(outward at theme tick length).}
}
\value{
A list of ggplot2 annotation layers.
}
\description{
Draws axis ticks at specified break positions, with style defaults taken from
the \code{axis.ticks} element of the set theme. Requires
\code{coord_cartesian(clip = "off")}.
}
\seealso{
\code{\link[=axis_line]{axis_line()}}, \code{\link[=axis_text]{axis_text()}},
\code{\link[=axis_bracket]{axis_bracket()}}, \code{\link[=reference_line]{reference_line()}},
\code{\link[=panel_shade]{panel_shade()}}, \code{\link[=sec_axis_text]{sec_axis_text()}}
}
