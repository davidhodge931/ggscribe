% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-panel-reference.R
\name{panel_grid}
\alias{panel_grid}
\title{Annotate panel gridlines}
\usage{
panel_grid(
  xintercept = NULL,
  yintercept = NULL,
  xmin = -Inf,
  xmax = Inf,
  ymin = -Inf,
  ymax = Inf,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  layout = NULL
)
}
\arguments{
\item{xintercept}{One or more x positions for vertical gridlines, in data
coordinates or wrapped in \code{\link[=I]{I()}} for normalised panel coordinates (npc).
May be a vector.}

\item{yintercept}{One or more y positions for horizontal gridlines, in data
coordinates or wrapped in \code{\link[=I]{I()}} for normalised panel coordinates (npc).
May be a vector.}

\item{xmin, xmax}{Left and right crop bounds. Vertical gridlines outside
\verb{[xmin, xmax]} are not drawn; horizontal gridlines run only from \code{xmin}
to \code{xmax}. Defaults to \code{-Inf} and \code{Inf} (full panel). Use \code{\link[=I]{I()}} for
normalised coordinates (npc). Note: filtering (removing lines outside the
range) only works when both the crop bound and the intercept are in data
coordinates. npc bounds affect extent only.}

\item{ymin, ymax}{Bottom and top crop bounds. Horizontal gridlines outside
\verb{[ymin, ymax]} are not drawn; vertical gridlines run only from \code{ymin} to
\code{ymax}. Defaults to \code{-Inf} and \code{Inf} (full panel). Use \code{\link[=I]{I()}} for
normalised coordinates (npc). Note: filtering only works when both the
crop bound and the intercept are in data coordinates. npc bounds affect
extent only.}

\item{colour}{Inherits from \code{panel.grid.major} in the set theme. May be a
vector the same length as the total number of lines.}

\item{linewidth}{Inherits from \code{panel.grid.major} in the set theme. Supports
\code{rel()}. May be a vector the same length as the total number of lines.}

\item{linetype}{Inherits from \code{panel.grid.major} in the set theme. May be a
vector the same length as the total number of lines.}

\item{layout}{Controls which panels the annotation appears in. \code{NULL}
(default) repeats in all panels. An integer targets a specific panel.
\code{"fixed"} repeats in all panels ignoring faceting variables. See
\code{\link[ggplot2:layer]{ggplot2::layer()}} for full details.}
}
\value{
A list of ggplot2 annotation layers.
}
\description{
Draws gridlines at specified positions, with style defaults taken from the
\code{panel.grid.major} element of the set theme. Crop bounds (\code{xmin}, \code{xmax},
\code{ymin}, \code{ymax}) both filter which lines are drawn and control how far they
run across the panel.
}
