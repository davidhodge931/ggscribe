% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panel.R
\name{annotate_panel_shade}
\alias{annotate_panel_shade}
\title{Annotate a shaded panel region}
\usage{
annotate_panel_shade(
  ...,
  xmin = -Inf,
  xmax = Inf,
  ymin = -Inf,
  ymax = Inf,
  fill = NULL,
  alpha = 0.25,
  colour = "transparent",
  linewidth = NULL,
  linetype = NULL
)
}
\arguments{
\item{...}{Not used. Allows trailing commas and named-argument style calls.}

\item{xmin, xmax}{Left and right edges of the rectangle. Defaults to \code{-Inf}
and \code{Inf}. Use \code{I()} for normalized coordinates (0-1).}

\item{ymin, ymax}{Bottom and top edges of the rectangle. Defaults to \code{-Inf}
and \code{Inf}. Use \code{I()} for normalized coordinates (0-1).}

\item{fill}{Fill colour. Defaults to a neutral grey that adapts to the panel
background luminance.}

\item{alpha}{Opacity of the rectangle. Defaults to \code{0.25}.}

\item{colour}{Border colour. Defaults to \code{"transparent"}.}

\item{linewidth}{Inherits from \code{panel.border} in the set theme. Supports
\code{rel()}.}

\item{linetype}{Border linetype. Defaults to \code{1}.}
}
\value{
A list containing an annotation layer.
}
\description{
Draws a filled rectangle over the panel with colour defaults taken from the
set theme. Defaults to a subtle overlay across the full panel, with the fill
automatically adapting to light or dark panel backgrounds.
}
\examples{
library(ggplot2)

set_theme(theme_classic())

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point()

# Shade the full panel
p + annotate_panel_shade()

# Shade a specific data range
p + annotate_panel_shade(xmin = 3, xmax = 4)

# Shade using normalized coordinates
p + annotate_panel_shade(xmin = I(0.25), xmax = I(0.75))

# Custom fill and opacity
p + annotate_panel_shade(ymin = 20, ymax = 30, fill = "steelblue", alpha = 0.15)
}
