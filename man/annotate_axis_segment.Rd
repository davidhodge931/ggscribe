% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-line-update.R
\name{annotate_axis_segment}
\alias{annotate_axis_segment}
\title{Annotate a straight segment between two data points}
\usage{
annotate_axis_segment(
  ...,
  x,
  y,
  xend,
  yend,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL
)
}
\arguments{
\item{...}{Not used. Allows trailing commas and named-argument style calls.}

\item{x, y}{Start coordinates of the segment.}

\item{xend, yend}{End coordinates of the segment.}

\item{colour}{Inherits from \code{axis.line} in the set theme.}

\item{linewidth}{Inherits from \code{axis.line} in the set theme. Supports
\code{rel()}.}

\item{linetype}{Inherits from \code{axis.line} in the set theme.}
}
\value{
A list containing a single ggplot2 annotation layer.
}
\description{
Draws a straight line segment between \verb{(x, y)} and \verb{(xend, yend)}, with
style defaults taken from the \code{axis.line} element of the set theme. For a
curved line, see \code{\link[=annotate_axis_curve]{annotate_axis_curve()}}. For axis-edge lines, see
\code{\link[=annotate_axis_line]{annotate_axis_line()}}.
}
\examples{
library(ggplot2)

set_theme(
  ggrefine::theme_grey(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  )
)

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  coord_cartesian(clip = "off")

# Segment between two data points
p + annotate_axis_segment(x = 2, y = 15, xend = 5, yend = 30)

# Thicker segment
p + annotate_axis_segment(x = 2, y = 15, xend = 5, yend = 30, linewidth = rel(2))
}
\seealso{
\code{\link[=annotate_axis_line]{annotate_axis_line()}}, \code{\link[=annotate_axis_curve]{annotate_axis_curve()}}
}
