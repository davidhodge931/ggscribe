% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-line-update.R
\name{annotate_axis_curve}
\alias{annotate_axis_curve}
\title{Annotate a curved line between two data points}
\usage{
annotate_axis_curve(
  ...,
  x,
  y,
  xend,
  yend,
  curvature = 0.3,
  angle = 90,
  ncp = 5,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL
)
}
\arguments{
\item{...}{Not used. Allows trailing commas and named-argument style calls.}

\item{x, y}{Start coordinates of the curve.}

\item{xend, yend}{End coordinates of the curve.}

\item{curvature}{Amount of curvature. Negative values curve left, positive
curve right, zero is straight. Defaults to \code{0.3}.}

\item{angle}{Skew angle of the curve control points (0–180). Defaults to
\code{90}.}

\item{ncp}{Number of control points. Higher values produce smoother curves.
Defaults to \code{5}.}

\item{colour}{Inherits from \code{axis.line} in the set theme.}

\item{linewidth}{Inherits from \code{axis.line} in the set theme. Supports
\code{rel()}.}

\item{linetype}{Inherits from \code{axis.line} in the set theme.}
}
\value{
A list containing a single ggplot2 annotation layer.
}
\description{
Draws a curved line between \verb{(x, y)} and \verb{(xend, yend)}, with style
defaults taken from the \code{axis.line} element of the set theme. For a straight
segment, see \code{\link[=annotate_axis_segment]{annotate_axis_segment()}}. For axis-edge lines, see
\code{\link[=annotate_axis_line]{annotate_axis_line()}}.
}
\examples{
library(ggplot2)

set_theme(
  ggrefine::theme_grey(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  )
)

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  coord_cartesian(clip = "off")

# Gentle curve between two data points
p + annotate_axis_curve(x = 2, y = 15, xend = 5, yend = 30, curvature = 0.3)

# Sharp curve in the opposite direction
p + annotate_axis_curve(x = 2, y = 15, xend = 5, yend = 30, curvature = -0.6)
}
\seealso{
\code{\link[=annotate_axis_line]{annotate_axis_line()}}, \code{\link[=annotate_axis_segment]{annotate_axis_segment()}}
}
