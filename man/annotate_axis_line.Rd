% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis.R
\name{annotate_axis_line}
\alias{annotate_axis_line}
\title{Annotate an axis line}
\usage{
annotate_axis_line(
  ...,
  position = NULL,
  x = NULL,
  y = NULL,
  xmin = NULL,
  xmax = NULL,
  ymin = NULL,
  ymax = NULL,
  xend = NULL,
  yend = NULL,
  curvature = NULL,
  angle = 90,
  ncp = 5,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  element_to = "keep"
)
}
\arguments{
\item{...}{Not used. Allows trailing commas and named-argument style calls.}

\item{position}{One of \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"right"}. Axis
line mode only.}

\item{x}{In axis line mode, a single x value for a vertical line. In
segment/curve mode, the x start position. Use \code{I()} for normalized
coordinates (0-1).}

\item{y}{In axis line mode, a single y value for a horizontal line. In
segment/curve mode, the y start position. Use \code{I()} for normalized
coordinates (0-1).}

\item{xmin, xmax}{Start and end x positions for a horizontal axis line. Use
\code{I()} for normalized coordinates (0-1). Axis line mode only.}

\item{ymin, ymax}{Start and end y positions for a vertical axis line. Use
\code{I()} for normalized coordinates (0-1). Axis line mode only.}

\item{xend, yend}{End position of the segment or curve. Providing all of
\code{x}, \code{y}, \code{xend}, \code{yend} triggers segment/curve mode.}

\item{curvature}{Amount of curvature. Negative curves left, positive curves
right, zero is straight. \code{NULL} (default) draws a straight segment.}

\item{angle}{Skew angle of curve control points (0-180). Used only when
\code{curvature} is non-\code{NULL}. Defaults to \code{90}.}

\item{ncp}{Number of curve control points. Higher values give smoother
curves. Used only when \code{curvature} is non-\code{NULL}. Defaults to \code{5}.}

\item{colour}{Inherits from \code{axis.line} in the set theme.}

\item{linewidth}{Inherits from \code{axis.line} in the set theme. Supports
\code{rel()}.}

\item{linetype}{Inherits from \code{axis.line} in the set theme.}

\item{element_to}{One of \code{"keep"}, \code{"transparent"}, or \code{"blank"}. Controls
whether the native theme axis line is suppressed. Defaults to \code{"keep"}.
Axis line mode only.}
}
\value{
A list of ggplot2 annotation layers and theme elements.
}
\description{
Draws a line along an axis edge or between two arbitrary points, with style
defaults taken from the \code{axis.line} element of the set theme. Requires
\code{coord_cartesian(clip = "off")}.
}
\details{
Operates in two modes. The primary \strong{Axis line mode} of a horizontal/vertical
line is triggered by \code{position}, \code{x}, or \code{y} alone. The secondary
\strong{Segment/curve mode} is triggered when \code{x}, \code{y}, \code{xend}, and \code{yend} are
all provided. Pass \code{curvature} etc to draw a curve instead of a straight line
(with axis line defaults).
}
\examples{
library(ggplot2)

set_theme(theme_classic())

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  coord_cartesian(clip = "off")

# Replace the bottom axis line
p + annotate_axis_line(position = "bottom", element_to = "transparent")

# Partial bottom axis between x = 2 and x = 4
p + annotate_axis_line(position = "bottom", xmin = 2, xmax = 4, element_to = "transparent")

# Vertical rule at x = 3.5
p + annotate_axis_line(x = 3.5)

# Straight line between two data points
p + annotate_axis_line(x = 2, y = 15, xend = 5, yend = 30)

# Curved line between two data points
p + annotate_axis_line(x = 2, y = 15, xend = 5, yend = 30, curvature = 0.3)
}
