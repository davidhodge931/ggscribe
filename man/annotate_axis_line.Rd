% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axis-line-update.R
\name{annotate_axis_line}
\alias{annotate_axis_line}
\title{Annotate an axis line}
\usage{
annotate_axis_line(
  ...,
  position = NULL,
  x = NULL,
  y = NULL,
  xmin = NULL,
  xmax = NULL,
  ymin = NULL,
  ymax = NULL,
  colour = NULL,
  linewidth = NULL,
  linetype = NULL,
  elements_to = "keep"
)
}
\arguments{
\item{...}{Not used. Allows trailing commas and named-argument style calls.}

\item{position}{One of \code{"top"}, \code{"bottom"}, \code{"left"}, or \code{"right"}. Inferred
from \code{x} or \code{y} if not provided.}

\item{x}{A single x value for a vertical reference line. Use \code{I()} for
normalized coordinates (0-1).}

\item{y}{A single y value for a horizontal reference line. Use \code{I()} for
normalized coordinates (0-1).}

\item{xmin, xmax}{Start and end x positions for a horizontal axis line. Use
\code{I()} for normalized coordinates (0-1).}

\item{ymin, ymax}{Start and end y positions for a vertical axis line. Use
\code{I()} for normalized coordinates (0-1).}

\item{colour}{Inherits from \code{axis.line} in the set theme.}

\item{linewidth}{Inherits from \code{axis.line} in the set theme. Supports
\code{rel()}.}

\item{linetype}{Inherits from \code{axis.line} in the set theme.}

\item{elements_to}{One of \code{"keep"}, \code{"transparent"}, or \code{"blank"}. Controls
whether the native theme axis line is suppressed. Defaults to \code{"keep"}.}
}
\value{
A list of ggplot2 annotation layers and theme elements.
}
\description{
Draws a line along an axis edge or as an interior reference line, with style
defaults taken from the \code{axis.line} element of the set theme. Use
\code{xmin}/\code{xmax} or \code{ymin}/\code{ymax} to draw a partial line. Lines along or
outside the panel boundary require \code{coord_cartesian(clip = "off")}.
}
\details{
To draw a straight or curved line between two arbitrary data points, see
\code{\link[=annotate_axis_segment]{annotate_axis_segment()}} and \code{\link[=annotate_axis_curve]{annotate_axis_curve()}}.
}
\examples{
library(ggplot2)

set_theme(
  ggrefine::theme_grey(
    panel_heights = rep(unit(50, "mm"), 100),
    panel_widths = rep(unit(75, "mm"), 100),
  )
)

p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  coord_cartesian(clip = "off")

# Replace the bottom axis line
p + annotate_axis_line(position = "bottom", elements_to = "transparent")

# Partial bottom axis between x = 2 and x = 4
p + annotate_axis_line(position = "bottom", xmin = 2, xmax = 4, elements_to = "transparent")

# Vertical reference line at x = 3.5
p + annotate_axis_line(x = 3.5)
}
\seealso{
\code{\link[=annotate_axis_segment]{annotate_axis_segment()}}, \code{\link[=annotate_axis_curve]{annotate_axis_curve()}}
}
